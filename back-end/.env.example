# Copia este archivo como .env y completa los valores
# Para desarrollo local:
DB_USER=root
DB_PASSWORD=tu_password_aqui
DB_HOST=localhost
DB_PORT=3306
DB_NAME=bi_empleabilidad

# Para Railway (estos se ponen en el panel de Railway, NO en .env):
# DB_USER=      → copiar de Railway MySQL
# DB_PASSWORD=  → copiar de Railway MySQL
# DB_HOST=      → copiar de Railway MySQL
# DB_PORT=      → copiar de Railway MySQL
# DB_NAME=      → copiar de Railway MySQL
